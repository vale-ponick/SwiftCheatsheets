# Задачи: цепочки методов (filter, map, sorted)

## Задача 1
Дан массив строк `["swift", "xcode", "playground", "debug", "compiler"]`.
Оставь слова длиннее 5 символов, приведи к верхнему регистру, отсортируй по алфавиту.

## Задача 2
Дан массив чисел `[3, 7, 0, -5, 8, -2, 10, -1, 4]`.
Оставь чётные числа и возведи их в квадрат.

## Задача 3
Дан массив чисел `[1, 2, 3, 4, 5, 6]`.
Оставь нечётные, увеличь каждый в 2 раза, отсортируй по убыванию.
2. dataCleaningTasks.md — задачи на очистку строк
markdown
# Задачи: очистка строк (trimming, allSatisfy, priority)

## Задача 1
Дан массив строк `["", "Swift", "  ", "iOS", " Dev ", "", "Closure"]`.
Очисти строки от пробелов по краям, отбрось пустые, преобразуй длину в `"short"` (≤4) или `"long"` (>4), отсортируй по алфавиту.

## Задача 2
Дан массив строк `[" hello ", "world", " Swift ", "  ", "iOS", " Dev ", "", "   ", "cLOSURE"]`.
Очисти строки, отбрось пустые, проверь, что строка состоит только из букв, классифицируй как `"shortWord"` (≤4), `"longWord"` (>4) или `"notWord"`. Отсортируй: сначала `shortWord`, потом `longWord`, потом `notWord`.
3. deepOptionalTasks.md — задачи на глубокие опционалы
markdown
# Задачи: глубокие опционалы (`[Int?]?`)

## Задача 1
Напиши функцию `sumEven(in:) -> Int`, которая:
- принимает `[Int?]?`
- убирает `nil` на обоих уровнях
- суммирует чётные числа
- если массива нет или чётных нет — возвращает `0`

## Задача 2
Напиши функцию `firstPositive(in:) -> Int?`, которая:
- принимает `[Int?]?`
- возвращает первое положительное число или `nil`, если его нет